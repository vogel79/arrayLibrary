import java.io.BufferedReader;
import java.io.IOException;

public class LibraryTest {
    public static void main(String[] args) throws IOException {
        //   System.out.println("LibraryTest.main");
        LibraryController libraryController = new LibraryController();
        libraryController.readConsole();
      /*  AuthorService authorService = new AuthorService();
        Author author = new Author();
        String name = "a1";
        author.setFirstName(name);
        String surname = "A";
        author.setLastName(surname);
        authorService.createAuthor(author);
            author = new Author();
            name = "a1";
            author.setFirstName(name);
            surname = "A";
            author.setLastName(surname);
            authorService.createAuthor(author);
        //    System.out.println("Автор " + name + " " + surname + " создан");
       //     System.out.println("Автор " + ANSI_GREEN + name + " " + surname + ANSI_RESET + " создан");*/
    }
}
